# MAGCE_WMBrainChart


Model that was run was the DecoupledMAGCE class in the architecture.py file. The rest are different, non-final iterations.

Code that was used to run the models can be found in train.py

model_inference.py was used for evaluating the DL models.
